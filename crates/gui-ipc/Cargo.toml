[package]
name = "ghoststream-gui-ipc"
version = "0.1.0"
edition = "2021"
description = "Shared line-protocol types between ghoststream-gui and ghoststream-helper"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
